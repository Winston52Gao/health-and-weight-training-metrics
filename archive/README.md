# Archive

This folder stores legacy scripts, retired model artifacts, and historical experiment outputs.

## Contents

- `train_models_original.py`: the old combined training pipeline before the production refactor
- `scripts/`: legacy preprocessing and experiment scripts
- `models/model_A/`: archived Model A artifacts from the workout progression classifier
- `models/model_C/experiments/`: archived Model C ablation, benchmark, and stacked-model outputs

## Notes

- Production code now lives in `src/`
- Production Model C artifacts live in `models/model_C/`
- Experimental notebooks remain in `notebooks/experiments/`
